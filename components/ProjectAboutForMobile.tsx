import { projectDetails } from "@/projectDetails";
import React from "react";

const ProjectAboutForMobile = ({}: { project: (typeof projectDetails)[0] }) => {
  return <div>Details</div>;
};

export default ProjectAboutForMobile;
