const Navbar = () => {
  return (
    <div className="flex justify-end">
      <div className="flex gap-8">
        <button>Home</button>
        <button>Projects</button>
        <button>Info</button>
      </div>
    </div>
  );
};

export default Navbar;
